# fallback

Reserved for fallback routing logic.

Typical future concerns:

- fallback from preferred provider to secondary provider
- fallback after cooldown trigger
- fallback from local provider to remote provider
