# allocator

Reserved for runtime selection and assignment logic.

Typical future concerns:

- choose an idle runtime
- spawn a new runtime when capacity allows
- respect provider-level limits
- avoid cooled providers or instances
