# camoufox

Reserved repository slot for the local Camoufox fork checkout.

Expected remote topology:

- `origin` -> local GitHub fork
- `upstream` -> `https://github.com/daijro/camoufox`

Current state:

- placeholder slot only
- actual fork / checkout is not performed yet
