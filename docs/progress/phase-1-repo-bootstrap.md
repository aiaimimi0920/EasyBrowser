# Phase 1: Repository Bootstrap

- [x] P1-1 Create the public `EasyBrowser` target skeleton and root layout. Acceptance criteria: target repo exists; root structure matches the monorepo plan; source workspace remains untouched.
- [x] P1-2 Document the copy-only mapping, migration contract, and contributor-facing repository identity. Acceptance criteria: old-to-new mapping is explicit; source workspace is clearly marked read-only.
- [x] P1-3 Define root config and root operator script conventions. Acceptance criteria: root `config.example.yaml` exists; config ownership and script naming are documented.

## Notes

- Completed during the initial bootstrap pass on 2026-04-30.
- Keep all later implementation work in the new repo.
