# Root operator config for EasyBrowser.
#
# This file is intentionally minimal during the planning phase. Real sections
# will be expanded as modules are imported into the monorepo.

serviceBase:
  runtime:
    listen: "127.0.0.1:18080"
    launchMode: "docker"
    dockerNetwork: "EasyAiMi"
    dockerComposeProject: "easy-browser"
    runtimePool:
      enabled: true
      reconcileSeconds: 5
      idleTimeoutSeconds: 120
      providers:
        chrome:
          minWarm: 1
        camoufox:
          minWarm: 0
        geekez:
          minWarm: 0
        browserbase:
          minWarm: 0

chromeRuntime:
  headless: true
  useUndetectedChromedriver: false
  dockerImage: "easy-browser/chrome-runtime:local"
  binaryPath: ""
  chromedriverPath: ""
  pythonPath: ""

camoufoxRuntime:
  dockerImage: "easy-browser/camoufox-runtime:local"
  pythonPath: ""
  headless: true
  os: "windows"
  readyTimeoutMs: 75000

geekezRuntime:
  dockerImage: "easy-browser/geekez-runtime:local"
  pythonPath: ""
  readyTimeoutMs: 90000

browserbase:
  apiKey: ""
  projectId: ""

publishing:
  ghcr:
    registry: "ghcr.io"
    namespace: ""
    imageName: "easybrowser-service"
  importCode:
    syncEnabled: true
    syncIntervalSeconds: 7200
